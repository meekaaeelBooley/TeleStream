apiVersion: 1

providers:
  - name: TeleStream
    folder: TeleStream
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: false
    options:
      path: /var/lib/grafana/dashboards
